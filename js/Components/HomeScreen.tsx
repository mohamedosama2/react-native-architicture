import React, { useState } from 'react'
import { Button, Dimensions, SafeAreaView, StyleSheet, View, Text } from 'react-native'

export default function HomeScreen() {
  return (
    <SafeAreaView>
      <Text>Hellow World</Text>
    </SafeAreaView>
  )
}
